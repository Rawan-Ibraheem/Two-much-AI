PORT=3000
# Host port is 5433, not the Postgres default 5432: docker-compose.yml maps
# 5433:5432 because a local system Postgres already owns 5432 on this machine.
DATABASE_URL=postgresql://dawasearch:dawasearch@localhost:5433/dawasearch_dev
