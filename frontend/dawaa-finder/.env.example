# Base URL of the medicine search API (backend/server.js).
# Only non-secret values belong in a VITE_ variable - they are inlined into the
# browser bundle. The Anthropic key lives in the backend, never here.
VITE_API_BASE_URL=http://localhost:3000
